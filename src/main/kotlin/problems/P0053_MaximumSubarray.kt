package problems

class P0053_MaximumSubarray {
    fun maxSubArray(nums: IntArray): Int {
        var maxSum = nums[0]
        for (index in nums.indices) {
            
        }
        return maxSum
    }
}
